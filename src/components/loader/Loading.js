import React from 'react';
import loadingImg from '../../assets/Blocks-1s-131px .gif';

const Loading = () => {
    return (
        <div>
           <img src={loadingImg} alt="" />
        </div>
    );
};

export default Loading;